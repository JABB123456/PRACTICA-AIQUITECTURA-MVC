[project]
name = "finanzas-personales"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "JABB123456", email = "jorgeberba@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
]

[project.scripts]
finanzas-personales = "finanzas_personales:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
